const score = 100
//console.log(score);

const balance = new Number(100)
//console.log(balance);

//console.log(balance.toString());
//console.log(balance.toString().length);
//console.log(balance.toFixed(2));

const otherNumber = 25.7895
//console.log(otherNumber.to Precision(3)); // important point this can be asked in interview

const hundreds = 1000000
//console.log(hundreds.toLocaleString()); // this gives value in US numbers , 

//console.log(hundreds.toLocaleString('en-IN')); //but if you want value to be in indian numbers then use this


// Min and Max value is used in DSA in numbers

//****************************Maths ***********************/

console.log(Math); // math is an object and when you console log in inspect it will show you more values.

console.log(Math.abs(-4)); // gives absolute values (negative sign will show positive value but positive will be same )

console.log(Math.round(5.6));
console.log(Math.ceil(5.2)); // ceil uses ceiling/top value
console.log(Math.floor(5.4)); // floor uses ceiling/lowest  value

console.log(Math.random); // most important and this gives value always in between of 0-1.






 


 

